'use strict';

/**
 * crypto-wallet-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::crypto-wallet-type.crypto-wallet-type');
